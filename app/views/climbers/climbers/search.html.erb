<div class="container">
  <h1>検索結果</h1>
  <%= form_with url: climber_search_path, method: :get, local: true do |f| %>
    <%= f.text_field :name,{placeholder: "名前で検索"} %>
    <%= f.submit :search %>
  <% end %>
  <%= render 'climbers', climbers: @climbers %>
</div>